/**
 * 디버그 명령의 접두사.
 *
 * 슬래시 커맨드가 아니라 **그냥 메시지**다. 이유가 있다 —
 *   - 슬래시 커맨드는 디스코드에 등록해야 하고, 등록하면 목록에 보인다.
 *     디버그는 봇 주인만 쓰는 것이라 목록에 낄 이유가 없다.
 *   - 3초 안에 응답해야 한다는 제약도 없다.
 *   - 봇이 이상할 때 쓰는 것이라, 커맨드 등록이 깨져도 여전히 통해야 한다.
 */
export const PREFIX = "!y";

/** 디버그 버튼의 customId 네임스페이스. */
export const NAMESPACE = "debug";

/**
 * `!y 재시작` 이 쓰는 종료 코드.
 *
 * run.bat 은 이 코드를 「일부러 껐다 켜는 것」으로 보고 연속 실패로 세지 않는다.
 * 그냥 1 로 끝내면 재시도 횟수가 차서 다섯 번 만에 실행기가 멈춰 버린다.
 */
export const RESTART_EXIT_CODE = 26;
