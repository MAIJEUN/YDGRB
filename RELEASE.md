# 배포 자동화

푸시하면 GitHub Actions 가 알아서 빌드하고, YDGRB 버전을 붙여 릴리스를 만들고, 결과를 디스코드로 알립니다.

```
npm run ship -- "커밋 메시지"
   │
   ├ 타입 검사 · 빌드 (실패하면 여기서 멈춤 — 깨진 코드가 안 올라감)
   └ 커밋 → 푸시
        │
        └ GitHub Actions
             ├ 🟡 디스코드에 "배포 진행 중" 알림
             ├ npm ci → 타입 검사 → 빌드
             ├ 버전 계산 (예: 2026.725.0)
             ├ 릴리스 생성 + tar.gz 첨부
             └ 🟢/🔴 같은 알림 메시지를 결과로 갱신
```

알림은 **메시지를 새로 쌓지 않고 하나를 고칩니다.** 노랑으로 시작해 초록이나 빨강으로 바뀝니다.

---

## 버전 넘버링 (YDGRB)

`YYYY.MD.A` — 예: `2026.58.0` (2026년 5월 8일 첫 릴리스)

| 자리 | 의미 |
|------|------|
| `YYYY` | 연도 네 자리 |
| `MD` | 달과 일을 그대로 이어 붙인 값. 5월 8일 → `58`, 7월 25일 → `725`, 12월 31일 → `1231` |
| `A` | 그날 몇 번째 릴리스인지. **0부터** 시작하고 두 자리 이상도 됩니다 (`2026.58.10`) |

- 날짜가 바뀌면 앞자리가 달라지므로 `A` 는 자연히 0부터 다시 셉니다.
- 같은 날짜의 **가장 큰 A + 1** 을 씁니다. 태그를 지워도 번호를 재사용하지 않습니다
  (같은 버전이 서로 다른 코드를 가리키면 추적이 안 되므로).
- 날짜는 **한국 시간(KST)** 기준입니다. 러너 기본값은 UTC 라 워크플로에서 `TZ: Asia/Seoul` 을 지정해 뒀습니다.

> **알아 두실 점** — `MD` 에 0을 채우지 않기 때문에 **1월 25일과 12월 5일이 둘 다 `2026.125`** 가 됩니다.
> 규칙을 그대로 구현한 결과입니다. 구분이 필요하면 [scripts/version.mjs](scripts/version.mjs) 의
> `datePart` 에서 `String(month).padStart(2, "0")` 처럼 두 자리로 채우면 됩니다 (`2026.0125`).

다음에 붙을 버전이 궁금하면:

```bash
npm run version:next
```

---

## 커밋 메시지가 곧 릴리스 설명

릴리스 설명은 **지난 릴리스 이후의 커밋**으로 만들어집니다. 이렇게 쓰면 그대로 문서가 됩니다.

```
수정: 소원권 수락/거절 시 이미지 소멸 수정 — wish.ts:520 (decideWish)

증상: 수락/거절 버튼을 누르면 이미지가 즉시 사라짐.
원인: 임베드의 attachment:// 는 '이번 요청에서 올라가는 파일'만 가리킬 수 있는데,
      편집 요청에는 올릴 파일이 없어 참조가 끊김.
해결: 편집할 때 embeds/attachments 를 아예 보내지 않는다.
```

결과:

```markdown
## 수정
- `소원권 수락/거절 시 이미지 소멸 수정 — wish.ts:520 (decideWish)`
> 증상: 수락/거절 버튼을 누르면 이미지가 즉시 사라짐.
> 원인: 임베드의 attachment:// 는 '이번 요청에서 올라가는 파일'만 가리킬 수 있는데,
>       편집 요청에는 올릴 파일이 없어 참조가 끊김.
> 해결: 편집할 때 embeds/attachments 를 아예 보내지 않는다.
```

- **첫 줄** = `분류: 제목` → 제목이 백틱 항목이 됩니다
- **빈 줄 뒤 본문** = 인용문(`>`)이 됩니다. 줄바꿈과 들여쓰기가 그대로 유지됩니다
- 분류는 **추가 · 수정 · 변경 · 제거** (영문 `feat`/`fix`/`change`/`remove` 도 됩니다).
  접두사가 없으면 **기타** 로 묶입니다
- 섹션 순서는 추가 → 수정 → 변경 → 제거 → 기타
- 버전 제목(`# YDGRB2026.725.0`)은 넣지 않습니다 — 릴리스 화면이 이미 제목으로 보여 줍니다

지금 커밋들로 어떤 설명이 나올지 미리 보려면:

```bash
node scripts/release-notes.mjs
```

본문을 쓰려면 메시지 없이 실행해 편집기를 여세요.

```bash
npm run ship
```

한 줄이면 충분할 때:

```bash
npm run ship -- "수정: 랭킹 정렬 오류"
```

---

## 처음 한 번만 — 설정

원격 저장소는 이미 연결해 뒀습니다 (`origin` → `https://github.com/MAIJEUN/YDGRB.git`, 브랜치 `main`).
남은 것은 시크릿 등록과 첫 푸시입니다.

### 1. 디스코드 웹훅 만들기

알림을 받을 채널 → **채널 편집 → 연동 → 웹후크 → 새 웹후크** → **웹후크 URL 복사**.

### 2. GitHub 시크릿 등록

저장소 → **Settings → Secrets and variables → Actions → New repository secret**

| 이름 | 값 |
|------|-----|
| `DISCORD_WEBHOOK_URL` | 위에서 복사한 웹후크 URL |

> `GITHUB_TOKEN` 은 Actions 가 자동으로 넣어 줍니다. 따로 만들 필요 없습니다.

**봇 토큰(`DISCORD_TOKEN`)은 여기에 넣지 마세요.** 배포 자동화에는 필요 없고,
`.env` 는 `.gitignore` 에 있어서 저장소에 올라가지 않습니다.

**웹후크 URL 이 밖으로 새면** 그 URL 을 아는 누구나 채널에 글을 쓸 수 있습니다.
노출됐다 싶으면 채널 설정에서 웹후크를 지우고 새로 만든 뒤 시크릿만 갈아 끼우면 됩니다.

### 3. 첫 푸시

```bash
git add -A
```

```bash
git commit -m "추가: 소원권 봇 최초 커밋 [skip release]"
```

```bash
git push -u origin main
```

> `[skip release]` 를 넣으면 이번 푸시에서는 배포가 돌지 않습니다.
> 시크릿을 먼저 넣어 두었다면 빼고 푸시해서 바로 첫 릴리스를 만들어도 됩니다.

---

## 평소 사용

```bash
npm run ship -- "소원권 랭킹 정렬 수정"
```

검사에 실패하면 커밋도 푸시도 하지 않습니다. 통과하면 커밋·푸시하고, 나머지는 Actions 가 합니다.

배포 없이 올리고만 싶으면 커밋 메시지에 `[skip release]` 를 넣으세요.

```bash
npm run ship -- "메모 정리 [skip release]"
```

수동으로 배포를 돌리고 싶으면 저장소 → **Actions → 빌드 및 배포 → Run workflow**.

---

## 릴리스에 담기는 것

| 항목 | 값 |
|------|-----|
| 태그 | `2026.725.0` |
| 제목 | `YDGRB2026.725.0` |
| 첨부 파일 | `YDGRB2026.725.0.zip` |

zip 안에는 `dist/`, `package.json`, `package-lock.json`, `README.md`, `.env.example`,
버전 표식 `VERSION`, 그리고 실행기 `run.bat` 이 들어갑니다.

### 윈도우 — run.bat 더블클릭

zip 을 풀고 **`run.bat` 을 더블클릭**하면 끝입니다. 나머지는 알아서 합니다.

```
1. 봇 실행
2. 슬래시 커맨드 등록   (커맨드를 새로 만들거나 이름·옵션을 바꿨을 때만)
3. 종료
```

- **Node.js 확인** — 없으면 설치 주소를 안내하고 멈춥니다
- **`.env` 준비** — 없으면 `.env.example` 을 복사해 만들고 메모장을 열어 줍니다.
  토큰이 비어 있어도 같은 안내가 나옵니다
- **패키지 설치** — `node_modules` 가 없으면 `npm ci --omit=dev` 를 한 번 돌립니다
- **업데이트 확인** — 메뉴가 뜨기 전에 물어봅니다 (아래)
- **자동 재시작** — 봇이 예기치 않게 꺼지면 10초 뒤 다시 켭니다.
  5번 연속 실패하면 멈추고 오류를 보여 줍니다 (토큰이 틀린 경우 무한 재시도를 막기 위해)

작업 스케줄러나 바로가기에서 메뉴 없이 바로 띄우려면 인자를 주세요.
이때는 물어볼 사람이 없으므로 업데이트 확인도 건너뜁니다.

```
run.bat bot
```

> 24시간 운영이라면 `run.bat` 보다 PM2 나 작업 스케줄러 쪽이 낫습니다.
> `run.bat` 의 재시작은 어디까지나 「가끔 꺼질 때」 대비용입니다.

### 업데이트 — 물어보고 받습니다

메뉴가 뜨기 전에 GitHub 최신 릴리스의 태그를 zip 에 들어 있던 `VERSION` 과 견줍니다.
다르면 이렇게 묻고, **Y 를 눌러야만** 받습니다.

```
 ------------------------------------------
   새 버전이 있습니다
     지금  YDGRB2026.729.0
     최신  YDGRB2026.730.0
 ------------------------------------------

 지금 업데이트할까요? (Y/N):
```

받겠다고 하면 **그 창에서 그대로** 받아 풀고 덮어씁니다. 새 창은 뜨지 않습니다.
새 버전에서 의존성이 늘었을 수 있으니 다시 시작하면서 `npm ci --omit=dev` 를 한 번 더 돌립니다.

- **`.env` 와 `data/` 는 그대로입니다** — zip 에 없으니 덮어쓰기에 휩쓸리지 않습니다
- **버전은 받은 태그를 직접 적습니다** — zip 에 `VERSION` 이 빠져 있어도 어긋나지 않게
- **N 이면 그냥 넘어갑니다** — 다음에 켤 때 다시 물어봅니다
- **인터넷이 막혀 있어도 멈추지 않습니다** — 5초 안에 답이 없으면 확인을 포기하고 진행합니다
- **`VERSION` 이 없으면 확인 자체를 건너뜁니다** — 소스에서 바로 돌리는 경우

#### 실행 중인 `run.bat` 은 어떻게 바꾸나

덮어쓸 수 없는 파일은 **`run.bat` 하나뿐**입니다. 그래서 압축을 푼 뒤 `run.bat` 만
`run.bat.new` 로 빼 두고, 나머지는 그 자리에서 바로 옮깁니다. 마지막에 이 한 줄이 남습니다.

```bat
if exist "%CD%\run.bat.new" move /y "%CD%\run.bat.new" "%~f0" >nul & cmd /d /s /c ""%~f0" %*" & exit
```

**한 줄인 것이 핵심입니다.** cmd 는 배치를 줄 단위로 읽어 들인 뒤 실행하므로, 이 줄을
읽고 난 다음에는 `run.bat` 이 바뀌어도 뒤따르는 명령이 흔들리지 않습니다. 줄을 나누면
바뀐 파일을 옛 바이트 위치부터 다시 읽어 엉뚱한 것을 실행하게 됩니다.
같은 이유로 맨 위 프롤로그의 `cmd /d /s /c … & exit /b` 도 한 줄이어야 합니다.

`start` 를 쓰지 않으므로 자식 cmd 가 같은 콘솔을 물려받습니다 — 그래서 창이 그대로입니다.
`exit /b` 가 아니라 `exit` 인 것도 이유가 있습니다. 이 대목은 `call :check_update` 로
불려 온 서브루틴 안이라, `exit /b` 면 메뉴로 돌아가 이미 바뀐 파일을 옛 위치부터 읽습니다.

> 이 방식은 **새 `run.bat` 을 손에 넣은 다음부터** 적용됩니다. 지금 배포돼 있는 zip 에는
> 예전 `run.bat`(도우미 방식)이 들어 있어서, 다음 한 번은 창이 한 번 더 뜹니다.

### 리눅스 · 직접 실행

```bash
unzip YDGRB2026.725.0.zip -d ydgrb
```

```bash
npm ci --omit=dev
```

`.env` 를 채우고 `node dist/index.js` 로 실행합니다.
슬래시 커맨드 등록은 `node dist/deploy-commands.js` 입니다.

소원권 데이터(`data/wishes.json`)는 릴리스에 들어가지 않으니 서버에 있는 것을 그대로 두세요.

---

## 구성 파일

| 파일 | 역할 |
|------|------|
| [.github/workflows/release.yml](.github/workflows/release.yml) | 빌드 → 버전 → 릴리스 → 알림 |
| [scripts/version.mjs](scripts/version.mjs) | YDGRB 버전 계산 |
| [scripts/release-notes.mjs](scripts/release-notes.mjs) | 커밋에서 릴리스 설명 생성 |
| [scripts/notify-discord.mjs](scripts/notify-discord.mjs) | 디스코드 웹훅 알림 (시작/결과) |
| [scripts/ship.mjs](scripts/ship.mjs) | 로컬에서 검사 후 커밋·푸시 |

`scripts/` 는 TypeScript 빌드 대상이 아닙니다. CI 에서 `npm ci` 전에도 돌아야 해서
의존성 없는 순수 Node 스크립트(`.mjs`)로 두었습니다.

---

## 문제가 생기면

| 증상 | 원인 |
|------|------|
| 디스코드 알림이 안 옴 | `DISCORD_WEBHOOK_URL` 시크릿 미등록 또는 웹훅 삭제됨. Actions 로그에 `알림을 건너뜁니다` / `알림 실패` 가 남습니다 |
| 알림은 오는데 릴리스가 없음 | 빌드나 타입 검사가 실패한 것. 알림이 빨강이고 「실행 로그」 링크에 이유가 있습니다 |
| `Resource not accessible by integration` | 저장소 → Settings → Actions → General → Workflow permissions 를 **Read and write** 로 |
| 버전 날짜가 하루 어긋남 | 워크플로의 `TZ: Asia/Seoul` 이 지워졌는지 확인 |
| 같은 날인데 A 가 0으로 돌아감 | `fetch-depth: 0` 이 빠지면 태그를 못 읽습니다 |
| 알림에 진행중만 뜨고 안 바뀜 | 마지막 「결과 알림」 단계가 `if: always()` 인지 확인 |

배포 자체가 실패해도 알림 전송 실패로 워크플로가 죽지는 않습니다 —
알림 스크립트는 오류를 경고로만 남기고 정상 종료합니다.
